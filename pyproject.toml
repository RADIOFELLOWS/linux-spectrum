[project]
name = "linux-spectrum"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.15.1",
]

[project.scripts]
linux-spectrum-cli = "cli:app"
